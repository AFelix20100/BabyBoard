{% extends 'base.html.twig' %}

{% block title %}BabyBoard | Information sur le joueur {{ player.pseudo }}{% endblock %}

{% block body %}
    <div class="site-section">
        <div class="container mt-5">
            <div class="row d-flex justify-content-center">
                <div class="col-7 col-sm-12 mt-5">
                    <a href="{{ path('app_player_index') }}">Retour à la liste</a>
                    <table class="table custom-table">
                        <tbody class="text-center">
                        <tr>
                            <td>PSEUDO</td>
                            <td>{{ player.pseudo }}</td>
                        </tr>
                        <tr>
                            <td>REJOINT LE</td>
                            <td>{{ player.createdAt ? player.createdAt|date('d/m/Y') : '' }}</td>
                        </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

