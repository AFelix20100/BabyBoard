{% extends 'base_dashboard.html.twig' %}

{% block title %}BabyBoard | Ajouter un participant{% endblock %}

{% block body %}
    <div class="site-section">
      <div class="container mt-5">
        <div class="row d-flex justify-content-center">
            <div class="col-lg-7 col-sm-12" style="margin-top: 75px;">
                <a href="{{ path('app_dashboard_team_composition_show', {id : team.id}) }}">Retour à la liste</a>
                <h1>Ajouter un participant</h1>
                {{ include('dashboard/team_composition/_form.html.twig') }}
            </div>
        </div>
      </div>
    </div>
{% endblock %}
