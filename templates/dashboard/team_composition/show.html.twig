{% extends 'base_dashboard.html.twig' %}

{% block title %}TeamComposition{% endblock %}

{% block body %}
    <h1>TeamComposition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team_composition.id }}</td>
            </tr>
            <tr>
                <th>IsHost</th>
                <td>{{ team_composition.isHost ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsGuest</th>
                <td>{{ team_composition.isGuest ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_team_composition_index') }}">back to list</a>

    <a href="{{ path('app_team_composition_edit', {'id': team_composition.id}) }}">edit</a>

    {{ include('team_composition/_delete_form.html.twig') }}
{% endblock %}
