
		<div class="row d-flex justify-content-center">
			<div class="col-lg-7 col-sm-12">
                {{ form_start(form) }}
				<div class="row">
					<div class="col-lg-6 col-sm-12">
						{{ form_row(form.RedTeam, {'attr': {'class': 'form-control js-example-basic-single'}}) }}
						{{ form_row(form.PointsRed, {'attr': {'class': 'form-control'}}) }}
					</div>
					<div class="col-lg-6 col-sm-12">
						{{ form_row(form.BlueTeam, {'attr': {'class': 'form-control js-example-basic-single'}}) }}
						{{ form_row(form.PointsBlue, {'attr': {'class': 'form-control'}}) }}
					</div>
				</div>
				{{ form_row(form.winnerTeam, {'attr': {'class': 'form-control js-example-basic-single'}}) }}
				<div class="d-flex justify-content-center">
                	<button class="btn btn-lg btn-primary mt-2">{{ button_label|default('Save') }}</button>
				</div>
			</div>
			{{form_end(form)}}
		</div>
	</div>
