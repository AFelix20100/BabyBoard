{% extends 'base_dashboard.html.twig' %}

{% block title %}Dashboard | Création d'une équipe{% endblock %}

{% block body %}
    
    <div class="site-section">
      <div class="container mt-5">
        <div class="row d-flex justify-content-center">
          <div class="col-lg-7 col-sm-12 mt-5">
            <a href="{{ path('app_dashboard_team_index') }}">Retour à la liste</a>
            <h1>Créer une nouvelle équipe</h1>
              {{ include('dashboard/team/_form.html.twig') }}
            </div>
        </div>
      </div>
    </div>
{% endblock %}
