{% extends 'base_dashboard.html.twig' %}

{% block title %}Dashboard | Modification de l'équipe {{team.name}}{% endblock %}

{% block body %}
    <div class="site-section">
      <div class="container mt-5">
        <div class="row d-flex justify-content-center">
          <div class="col-lg-7 col-sm-12 mt-5">
            <a href="{{ path('app_dashboard_team_index') }}">Retour à la liste</a>
            <h1>Modifier l'équipe</h1>
                {{ include('dashboard/team/_form.html.twig', {'button_label': 'Modifier'}) }}
                {{ include('dashboard/team/_delete_form.html.twig') }}
            </div>
        </div>
      </div>
    </div>
{% endblock %}
